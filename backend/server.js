// Deprecated JS server stub. The real server is implemented in TypeScript under `src/`.
// Keep this file present for compatibility, but it will exit if run accidentally.

console.error('Deprecated: use the TypeScript server at `tg-games/backend/src/server.ts`.');
process.exit(1);