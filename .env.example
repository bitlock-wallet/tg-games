# Example env file for running tg-games with docker-compose
# Replace placeholders with real values before running

# Database connection URL
# When running inside Docker Compose, use the service hostname `db` so containers can reach Postgres.
# For local (host) development you can override this to use localhost.
DATABASE_URL=postgres://postgres:postgres@db:5432/tg_games

# Telegram bot token (required for verification and bot operation)
BOT_TOKEN=<your-telegram-bot-token-here>

# Public HTTPS URL where the frontend is reachable by Telegram clients
# For local development, use an ngrok HTTPS URL like https://abcd-1234.ngrok.io
BASE_WEBAPP_URL=<your-public-https-url-here>

# Public HTTPS URL for backend SWAP API (if different from webapp URL)
SWAP_VOLUME_API_URL=<your-public-https-url-for-swap-api-here>

# Telegram InitData public key for verifying data integrity
# -- PRODUCTION --
#TELEGRAM_INITDATA_PUBLIC_KEY=e7bf03a2fa4602af4580703d88dda5bb59f32ed8b02a56c187fe7d34caed242d
# -- DEVELOPMENT --
TELEGRAM_INITDATA_PUBLIC_KEY=40055058a4ee38156a06562e52eece92a771bcd8346a8c4615cb7376eddf72ec

# Telegram secure bot app (short name registered in BotWFather - used for deep links)
TELEGRAM_APP_SHORTNAME=lumberbull

TELEGRAM_BOT_USERNAME=bitlockgames_test_bot

# Optional: Maximum allowed age (seconds) for initData auth_date
INITDATA_MAX_AGE=300

# Optional: Use mock multiplier for testing (set to 1 to enable)
# USE_MOCK_MULTIPLIER=1